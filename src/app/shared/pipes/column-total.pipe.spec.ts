import { ColumnTotalPipe } from './column-total.pipe';

describe('ColumnTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
